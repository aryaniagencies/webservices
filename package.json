{
  "name": "@your-scope/webservices",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "files": ["src", "prisma", "wrangler.toml"],
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "prisma:generate": "prisma generate",
    "dev": "wrangler dev",
    "deploy": "npm run prisma:generate && wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "hono": "^4.9.0",
    "marked": "^15.0.6",
    "sanitize-html": "^2.14.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "wrangler": "^4.36.0"
  }
}